version: "3.8"
services:
  database:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    environment:
      - 'POSTGRES_USER=acme'
      - 'POSTGRES_DB=acmedb'
      - 'POSTGRES_PASSWORD=acme'